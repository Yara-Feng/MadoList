/* components/task-card/index.wxss */

.task-card {
  padding: var(--spacing-sm) var(--spacing-md);
  margin-bottom: var(--spacing-sm);
}

.task-done {
  opacity: 0.7;
}

.task-title-done {
  text-decoration: line-through;
  color: var(--color-text-placeholder);
}

.checkbox {
  font-size: 40rpx;
  line-height: 1;
  flex-shrink: 0;
}
